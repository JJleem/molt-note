TASK-063이 만든 파일 (AC-6 — 제품 코드 변경 없음)

새로 만든 것 둘 (테스트만):
  src-tauri/tests/manual_handoff_invariants.rs   1016줄 · 15 tests
  tests/manual-handoff-invariants.test.ts         471줄 · 20 tests

수정한 제품 파일: 없음.
삭제하거나 약화한 테스트: 없음.

`git-status.txt`의 ` M` 목록과 나머지 `??` 목록은 **이 Run 이전부터 있던 것**이다 (Run 시작
시점의 git status와 같다 — TASK-055 ~ TASK-062의 산출물). 이 Task는 그 파일들을 한 줄도
건드리지 않았다: 불변이 지켜지지 않는 자리를 발견하면 제품 코드를 무르게 고치는 대신 그
사실을 드러내기로 되어 있고, 실제로 그렇게 다룬 자리가 하나 있다 —
`src/screens/aiProviderSettings.ts`의 provider 선택 목록이 벤더 이름을 글자로 담는다는 사실은
파일을 고치는 대신 **선언된 예외 하나로 못박았다** (`invariant-test-map.md`의 MH-6 항목).
