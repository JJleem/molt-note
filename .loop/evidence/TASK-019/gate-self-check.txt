$ node tools/loop-runtime/loopctl.mjs self-check build lint test
(2026-09-02 · TASK-019 · Attempt 2 · 최종 실행 결과 그대로)

Self-check (advisory — the runtime reruns gates independently after the worker finishes)
Gates: build, lint, test

[build] npm run build
[lint] npm run lint
[test] npm run test

build: PASS  exit=0  0.8s
lint: PASS  exit=0  1.1s
test: PASS  exit=0  6.9s

Self-check: all gates passed
Artifacts: .loop-local/self-check/  (advisory only — not a gate report)

---------------------------------------------------------------------------
Gate 명령 (.loop/project.yaml)

  build  npm run build   →  tsc && vite build
  lint   npm run lint    →  eslint . && cargo clippy --manifest-path src-tauri/Cargo.toml
                            --all-targets -- -D warnings
  test   npm run test    →  vitest run && cargo test --manifest-path src-tauri/Cargo.toml

---------------------------------------------------------------------------
이 Run 도중 관측된 중간 실패와 그 처리 (숨기지 않는다)

1) test FAIL exit=101 — `tests/microphone_permission.rs`가 `Arc<T>`에 외부 trait을 구현해
   orphan rule(E0117)에 걸렸다.
   → 지역 newtype(`SharedMicrophone` · `SharedPermission`)으로 바꿔 해결. 단언은 그대로다.

2) test FAIL exit=101 — 기존 `tests/capture.rs`의 두 테스트가 실패했다.

     a_device_that_cannot_be_opened_reaches_the_user_as_the_shared_failure_contract
       left: MicrophonePermission / right: AudioDevice
     a_failure_to_open_serializes_into_the_shape_the_frontend_reads
       left: "microphonePermission" / right: "audioDevice"

   원인은 이 Task가 도입한 폴백이다 — 권한을 확정하지 못한(Undetermined) 상태에서 장치를
   열지 못하면 권한 안내로 분류되며, 두 테스트는 기본 권한 경계(macOS에서 Undetermined)를
   쓰고 있었다.
   → 두 테스트에 `.with_microphone(GrantedPermission)`을 붙여 **보려는 상태를 값으로 명시**했다.
     단언은 하나도 바꾸지 않았고 삭제·skip·약화한 테스트가 없다. 폴백 경로 자체는
     `tests/microphone_permission.rs`의 별도 테스트가 판정한다.
