//! **옮겨졌다.** HTTP 계약은 2026-09-08에 [`crate::net::http`]로 갔다.
//!
//! 두 번째 사용자가 생겼기 때문이다 (`ai::anthropic`). 그 파일의 첫 줄은 처음부터
//! *"이 계약은 Notion을 모른다"* 고 적고 있었고, 이제 자리도 그 말과 같아졌다.
//!
//! 이 모듈은 재수출만 한다 — `notion` 안의 코드는 한 줄도 바뀌지 않았다.

pub use crate::net::http::*;
